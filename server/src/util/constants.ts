// sort order options for mongo queries
export enum SortOrder {
  Asc = 1,
  Desc = -1,
}
// 5 minute cookie session
export const COOKIE_MAX_AGE = 20 * 60 * 1000
// user starts with 100 million tokens
export const USER_STARTING_BALANCE = 100000000
